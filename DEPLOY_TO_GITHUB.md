# Быстрая инструкция по загрузке на GitHub Pages

## ✅ Все готово к загрузке!

Файлы подготовлены:
- ✅ Секреты удалены из документации
- ✅ `.env` защищен в `.gitignore`
- ✅ Все пути обновлены на Cloudinary
- ✅ `config.js` содержит только публичные URL

## Команды для загрузки:

### 1. Добавьте все файлы (кроме игнорируемых)
```bash
git add .
```

### 2. Проверьте, что добавляется
```bash
git status
```
**Убедитесь, что `.env` НЕ в списке!**

### 3. Сделайте коммит
```bash
git commit -m "Deploy portfolio website to GitHub Pages"
```

### 4. Если репозиторий еще не создан на GitHub:
```bash
# Создайте репозиторий на GitHub.com, затем:
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git branch -M main
git push -u origin main
```

### 5. Если репозиторий уже существует:
```bash
git push origin main
```

### 6. Включите GitHub Pages:
1. Перейдите в Settings → Pages
2. Source: Branch `main`, folder `/ (root)`
3. Сохраните

## 🌐 Ваш сайт будет доступен по адресу:
```
https://YOUR_USERNAME.github.io/YOUR_REPO/
```

## 📋 Файлы, которые будут загружены:
- ✅ Все HTML, CSS, JS файлы
- ✅ `portfolio_data.json` с Cloudinary URLs
- ✅ `config.js` (без секретов)
- ✅ Документация
- ✅ Скрипты Python (для разработки)

## ❌ Файлы, которые НЕ будут загружены (в .gitignore):
- `.env` - секреты
- `images/`, `icons/`, `alisa/` - медиафайлы
- `*.toe`, `*.aep` - файлы разработки
- `Backup/`, `Adobe After Effects Auto-Save/`

