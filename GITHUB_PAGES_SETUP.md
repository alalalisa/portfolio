# Инструкция по загрузке на GitHub Pages

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [GitHub.com](https://github.com)
2. Нажмите "New repository" (или "+" → "New repository")
3. Назовите репозиторий (например, `portfolio`)
4. Выберите "Public" (GitHub Pages работает только с публичными репозиториями в бесплатном плане)
5. НЕ добавляйте README, .gitignore или лицензию (они уже есть)
6. Нажмите "Create repository"

## Шаг 2: Инициализируйте Git (если еще не сделано)

Откройте терминал в папке проекта и выполните:

```bash
# Проверьте, инициализирован ли Git
git status

# Если Git не инициализирован, выполните:
git init
git config user.name "Ваше Имя"
git config user.email "ваш@email.com"
```

## Шаг 3: Добавьте файлы и сделайте первый коммит

```bash
# Добавьте все файлы (кроме тех, что в .gitignore)
git add .

# Проверьте, что добавляется (убедитесь, что .env НЕ в списке!)
git status

# Сделайте первый коммит
git commit -m "Initial commit: Portfolio website with Cloudinary integration"
```

## Шаг 4: Подключите к GitHub репозиторию

```bash
# Замените YOUR_USERNAME и YOUR_REPO на ваши данные
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Отправьте код на GitHub
git branch -M main
git push -u origin main
```

## Шаг 5: Включите GitHub Pages

1. Перейдите в ваш репозиторий на GitHub
2. Нажмите "Settings" (вверху справа)
3. В левом меню найдите "Pages"
4. В разделе "Source" выберите:
   - Branch: `main`
   - Folder: `/ (root)`
5. Нажмите "Save"

## Шаг 6: Дождитесь публикации

GitHub Pages обычно публикует сайт за 1-2 минуты. Ваш сайт будет доступен по адресу:
```
https://YOUR_USERNAME.github.io/YOUR_REPO/
```

## ✅ Проверка перед загрузкой

Убедитесь, что:
- [x] `.env` НЕ добавлен в Git (проверьте `git status`)
- [x] `portfolio_data.json` содержит Cloudinary URLs
- [x] `config.js` содержит только публичные URL (без секретов)
- [x] Все медиафайлы загружены в Cloudinary
- [x] Секреты удалены из документации (.md файлов)

## 🔒 Безопасность

- ✅ API Secret хранится только в `.env` (локально, не в Git)
- ✅ Публичные URL в `config.js` безопасны
- ✅ Все секреты защищены `.gitignore`

## 📝 Важные файлы для GitHub

**Обязательно загрузить:**
- `index.html`
- `styles.css`
- `script.js`
- `p5_3d_shapes.js`
- `config.js`
- `portfolio_data.json`
- `shapes_coordinates.json`
- `alisa1.csv`
- `.gitignore`
- `README.md`

**НЕ загружать (уже в .gitignore):**
- `.env` - секреты
- `images/`, `icons/`, `alisa/` - медиафайлы на Cloudinary
- `*.toe`, `*.aep` - файлы разработки

## 🎉 Готово!

После публикации ваш сайт будет доступен на GitHub Pages, а медиафайлы будут загружаться с Cloudinary CDN.

